name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permette di eseguire manualmente

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            cd /var/www/ticketapp
            
            # Pull ultime modifiche
            echo "📥 Pulling latest changes..."
            git pull origin main
            
            # Install dipendenze backend
            echo "📦 Installing backend dependencies..."
            cd /var/www/ticketapp/backend
            npm install --production
            
            # Install dipendenze frontend
            echo "📦 Installing frontend dependencies..."
            cd /var/www/ticketapp/frontend
            npm install
            
            # Build frontend
            echo "🔨 Building frontend..."
            npm run build
            
            # Restart backend con PM2
            echo "🔄 Restarting backend..."
            pm2 restart ticketapp-backend
            
            # Verifica stato
            echo "✅ Checking PM2 status..."
            pm2 status
            
            # Verifica che il server risponda
            echo "✅ Testing server..."
            sleep 3
            curl -f http://localhost:3001/api/health || echo "⚠️ Health check failed, but deployment completed"
            
            echo "🎉 Deploy completato con successo!"

