# Database Connection
DATABASE_URL=PLACEHOLDER_DATABASE_URL

# Porta backend (default 3001)
PORT=3001

# JWT Secret per autenticazione
JWT_SECRET=PLACEHOLDER_JWT_SECRET

# Email Configuration (opzionale per sviluppo locale)
EMAIL_USER=info@logikaservice.it
EMAIL_PASS=

# Google OAuth (opzionale per sviluppo locale)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=

# Frontend URL (opzionale)
FRONTEND_URL=http://localhost:3000

# Allowed Origins (opzionale)
ALLOWED_ORIGINS=http://localhost:3000
